{
  "name": "express-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "init": "npm run build && npm run start",
    "build": "docker build -t express-app .",
    "start": "docker-compose -p express-app up -d",
    "stop": "docker-compose -p express-app stop",
    "restart": "npm run stop && npm run start",
    "local:build": "tsc",
    "local:build:watch": "tsc --watch",
    "local:start": "nodemon dist"
  },
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "dotenv": "^16.0.0",
    "express": "~4.16.1",
    "http-errors": "~1.6.3",
    "morgan": "~1.9.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^17.0.23",
    "nodemon": "^2.0.15",
    "typescript": "^4.6.3"
  }
}
